let nomes:string[] = ['Leonardo', 'Abreu','Furação'];

nomes.forEach((nome: string) => {
    console.log(nome);
})

let cores: Array<string> = ['amarelo', 'vermelho', '1'] ;

cores.forEach((cores:string) => {
    console.log(cores);
})

let arrayQuecabeTudo: Array<any> = ['amarelo', 'vermelho', 1, true] ;

arrayQuecabeTudo.forEach((qualquer:string) => {
    console.log(qualquer);
})